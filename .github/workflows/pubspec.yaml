name: whatsmind
description: Your WhatsApp, remembered
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  receive_sharing_intent: ^1.4.5

flutter:
  uses-material-design: true
  
